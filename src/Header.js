const Header = (props) => {
    return (
        <div class="header">
        <h1>Find Money </h1>
        <p>win money instantly with <strong>OnliBet</strong> we build on trust</p>
        </div>
    )
};

export default Header;